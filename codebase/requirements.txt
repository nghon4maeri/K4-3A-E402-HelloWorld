# FeedbackRadar — thư viện cần cho pipeline AI
#
# Cài:  python -m pip install -r codebase/requirements.txt

openai>=1.40.0           # SDK OpenAI-compatible — dùng để gọi DeepSeek
python-dotenv>=1.0.0     # đọc khoá API từ codebase/.env
